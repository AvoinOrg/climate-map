version: '3.1'

services:
  app:
    container_name: climate-map-app
    build: .
    env_file:
      - ./.env.local
      - ./.env.docker
    ports:
      - '${DEV_PORT}:3000'
    volumes:
      - .:/app:z
    entrypoint: '/bin/bash /app/docker-entrypoint.sh'
